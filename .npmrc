# Chainguard npm 12 blocks postinstall scripts by default (security)
# esbuild needs postinstall to fetch its native binary, so tsx watch fails
# without this. We trust our direct deps, so allow all.
allow-all-scripts=true
fund=false
audit=false
